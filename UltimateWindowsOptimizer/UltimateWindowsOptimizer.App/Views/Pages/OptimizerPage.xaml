<UserControl x:Class="UltimateWindowsOptimizer.App.Views.Pages.OptimizerPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <StackPanel>
        <TextBlock Text="Smart Optimizer" FontSize="24" FontWeight="SemiBold" Margin="0,0,0,8"/>
        <TextBlock Text="All available tweaks grouped by category. Each tweak shows risk, effect and can be applied or undone individually."
                   Foreground="{StaticResource TextSecondaryBrush}" TextWrapping="Wrap" Margin="0,0,0,20"/>

        <DockPanel Margin="0,0,0,12">
            <TextBox x:Name="TxtSearch" Width="280" Padding="8,6" VerticalContentAlignment="Center"
                     TextChanged="Search_Changed"/>
            <TextBlock DockPanel.Dock="Left" Text="Search: " VerticalAlignment="Center" Margin="0,0,8,0"/>
        </DockPanel>

        <ItemsControl x:Name="TweakList">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Style="{StaticResource Card}" Margin="0,0,0,10">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="14"/>
                                    <Border Background="{StaticResource BgHoverBrush}" CornerRadius="4" Padding="6,2" Margin="10,0,0,0">
                                        <TextBlock Text="{Binding RiskLevel}" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}"/>
                                    </Border>
                                </StackPanel>
                                <TextBlock Text="{Binding Description}" Foreground="{StaticResource TextSecondaryBrush}" TextWrapping="Wrap" Margin="0,4,0,0"/>
                                <TextBlock Text="{Binding ExpectedEffect}" FontSize="12" Foreground="{StaticResource SuccessBrush}" Margin="0,4,0,0"/>
                            </StackPanel>
                            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                                <Button Content="Apply" Style="{StaticResource PrimaryButton}" Margin="0,0,8,0"
                                        Tag="{Binding Id}" Click="Apply_Click"/>
                                <Button Content="Undo" Padding="12,8" Tag="{Binding Id}" Click="Undo_Click"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </StackPanel>
</UserControl>
